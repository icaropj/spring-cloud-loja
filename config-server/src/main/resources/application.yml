server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/icaropj/ms-config
          search-paths: fornecedor, loja, transportador
  application:
    name: 'config-server'
  boot:
    admin:
      client:
        url: http://localhost:9090